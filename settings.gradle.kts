pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "kotlinx-datetime-parcelize-issue"
include(":androidApp")
include(":shared")